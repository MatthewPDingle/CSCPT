// Centralized animation timing constants (in milliseconds)
export const CARD_STAGGER_DELAY_MS = 500;         // Delay between individual card reveals
export const POST_STREET_PAUSE_MS = 1000;         // Pause after a street reveal completes
export const POT_FLASH_DURATION_MS = 500;        // Duration of pot flash animation
export const CHIP_ANIMATION_DURATION_MS = 500;    // Duration for chip-to-pot and pot-to-player animations